name: Custom Email Action

on:
  push:
    branches: [ "main" ]
  workflow_dispatch: # Allows you to trigger it manually

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: 23f2000794@ds.study.iitm.ac.in
        run: echo "Hello, world!"

